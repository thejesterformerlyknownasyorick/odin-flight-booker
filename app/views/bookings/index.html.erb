<h1>Bookings</h1>

Upcoming Reservations:
<ul>
  <% @bookings.each do |booking| %>
    <li>
      <%= link_to "Booking ##{booking.id}", booking %> |
      Flying out of: <%= booking.flight.departure_airport.code %> |
      Flying to: <%= booking.flight.arrival_airport.code %> |
      Departure time: <%= booking.flight.date %> |
      Flight duration: <%= booking.flight.flight_duration %>
      Passengers: <% booking.passengers.each do |p| %>
        <%= p.name %>
      <% end %>
    </li>
  <% end %>
</ul>

<%= link_to "Find another flight", flights_path %>